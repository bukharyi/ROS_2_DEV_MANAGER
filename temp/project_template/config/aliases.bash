#!/bin/bash
# This file will be sourced by the dev_manager tool during workspace initialization.
# So you can add any alias that you want to be available in here.
# Example to run interface_bringup.bash script using alias:
alias r2link='source $WS_PROJECT_REPO/scripts/interface_bringup.bash'